info:
  name: Update application
  type: http
  seq: 9

http:
  method: PUT
  url: "{{host}}/api/applications/{{id}}"
  auth: inherit

runtime:
  variables:
    - name: id
      value: "34"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
