info:
  name: Get approved/reject by position
  type: http
  seq: 12

http:
  method: GET
  url: "{{host}}/api/approved-rejected/by-position"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
