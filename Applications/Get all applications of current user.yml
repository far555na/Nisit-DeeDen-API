info:
  name: Get all applications of current user
  type: http
  seq: 8

http:
  method: GET
  url: "{{host}}/api/applications/user"
  headers:
    - name: Authorization
      value: Bearer {{token}}
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: ""
  auth: inherit

runtime:
  variables:
    - name: id
      value: "50"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
