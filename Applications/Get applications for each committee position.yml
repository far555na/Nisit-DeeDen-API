info:
  name: Get applications for each committee position
  type: http
  seq: 11

http:
  method: GET
  url: "{{host}}/api/application/by-position"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
