info:
  name: Get applications for committee
  type: http
  seq: 8

http:
  method: GET
  url: "{{host}}/api/applications/committee"
  headers:
    - name: Authorization
      value: Bearer {{token}}
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
