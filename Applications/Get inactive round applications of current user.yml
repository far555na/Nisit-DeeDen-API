info:
  name: Get inactive round applications of current user
  type: http
  seq: 14

http:
  method: GET
  url: "{{host}}/api/application/user/inactive"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
