info:
  name: Get all categories
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/api/categories"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
