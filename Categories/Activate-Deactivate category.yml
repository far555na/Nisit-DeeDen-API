info:
  name: Activate/Deactivate category
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{host}}/api/categories/{{category-slug}}/toggle-status"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

runtime:
  variables:
    - name: category-slug
      value: study-bangkhen

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
