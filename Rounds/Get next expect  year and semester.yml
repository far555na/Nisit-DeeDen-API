info:
  name: Get next expect  year and semester
  type: http
  seq: 6

http:
  method: GET
  url: "{{host}}/api/rounds/next-expected"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
