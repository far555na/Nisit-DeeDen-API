info:
  name: Create new round
  type: http
  seq: 4

http:
  method: POST
  url: "{{host}}/api/rounds"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
