info:
  name: Delete round
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{host}}/api/rounds/{{id}}"
  auth: inherit

runtime:
  variables:
    - name: id
      value: "7"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
