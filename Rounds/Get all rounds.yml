info:
  name: Get all rounds
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/api/rounds"
  headers:
    - name: Authorization
      value: Bearer {{token}}
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
