info:
  name: Get all user from all domain
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}/api/users/all-domain"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
